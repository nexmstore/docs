---
title: "Event API: register and emit events"
sidebarTitle: "Overview"
description: "The Event API provides methods to register handlers and emit events across server, client, and an internal bus in NEXM Core."
---

The `Event` module lets you listen for events and emit them across boundaries. NEXM Core normalizes event handling so resources do not depend on raw FiveM events directly.

## Methods

<CardGroup cols={2}>
  <Card title="Event.On" href="/api/events/on">
    Register an event handler.
  </Card>

  <Card title="Event.Emit" href="/api/events/emit">
    Emit an event to targets.
  </Card>

  <Card title="Internal Bus" href="/api/events/internal-bus">
    Cross-resource event bus inside NEXM.
  </Card>
</CardGroup>

## Side

<Info>
  Server or Client availability per method: see individual method pages.
</Info>
